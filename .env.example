# Variables d'environnement pour Maplyo
# IMPORTANT: Copiez ce fichier vers .env et remplacez toutes les valeurs par défaut

# Port du serveur (défaut: 3001, Render utilise PORT dynamique)
PORT=3001

# Environnement (development/production)
NODE_ENV=development

# Clé secrète pour JWT - OBLIGATOIRE
# Générez une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NE JAMAIS utiliser cette valeur par défaut en production!
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_32_BYTE_HEX_STRING

# Base de données SQLite (gérée automatiquement)
# Le fichier sera créé dans database/maplyo.db

# Clés API de géocodage (optionnelles)
# GEOCODING_API_KEY=your_api_key_here
# MAPQUEST_API_KEY=your_mapquest_key_here
