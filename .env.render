# Configuration pour le déploiement Render
# Copiez ces variables dans les Environment Variables de votre service Render

# Port (automatiquement défini par Render)
# PORT=10000

# Sécurité JWT (OBLIGATOIRE - générez une clé secrète forte)
JWT_SECRET=your-super-secret-jwt-key-for-production-change-this-immediately

# Base de données (SQLite est incluse, pas besoin de configuration externe)
# DB_PATH sera automatiquement configuré

# Géocodage (Optionnel - pour améliorer les performances)
# GEOCODING_API_KEY=your-api-key-if-using-paid-service

# Environment
NODE_ENV=production

# Logging level pour production
LOG_LEVEL=info

# Timeout configurations pour Render
REQUEST_TIMEOUT=30000
GEOCODING_TIMEOUT=10000

# CORS configuration (ajustez selon votre domaine frontend)
# FRONTEND_URL=https://your-frontend-domain.com
